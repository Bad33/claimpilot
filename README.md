# claimpilot
ClaimPilot is an explainable AI claims triage backend that ingests claim documents, extracts structured fields, summarizes claim context, predicts claim complexity, recommends routing, and stores evidence-backed outputs with audit logs.
